*** Begin Patch
*** Update File: app/Services/WRService.php
@@
-                foreach ( as ) {
-                     = ->determineSlotIndexFromClock(->start_time, );
-                    if ( !== ) {
-                        continue;
-                    }
-
-                     = trim((string) (->title ?? '-'));
-                    [] = sprintf(' - %s',  ?: '-');
-
-                     = trim((string) (->output ?? ''));
-                    if ( !== '') {
-                        [] = ' - ' . ;
-                    }
-                }
+                foreach ( as ) {
+                     = [][0];
+                       = [][1];
+                    if (->start_time && ->end_time) {
+                        if (!->timesOverlap(->start_time, ->end_time, , )) {
+                            continue;
+                        }
+                    } else {
+                         = ->determineSlotIndexFromClock(->start_time, );
+                        if ( !== ) {
+                            continue;
+                        }
+                    }
+
+                     = trim((string) (->title ?? '-'));
+                    [] = sprintf(' - %s',  ?: '-');
+
+                     = trim((string) (->output ?? ''));
+                    if ( !== '') {
+                        [] = ' - ' . ;
+                    }
+                }
@@
-                ->setCellValue('E' . ,  ? implode("\n", ) : null);
-                ->setCellValue('G' . ,  ? implode("\n", ) : null);
+                ->setCellValue('E' . ,  ? implode("\n", ) : null);
+                ->setCellValue('G' . ,  ? implode("\n", ) : null);
+                // Ensure row height considers both E/F and G
+                 = is_array() ? count() : 0;
+                 = is_array() ? count() : 0;
+                 = max(, );
+                if ( > 0) {
+                     = max(20, min(230,  * 22));
+                    ->getRowDimension()->setRowHeight();
+                }
*** End Patch